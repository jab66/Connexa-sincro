INSERT INTO fnd_product(
        id, 
        base_price, 
        description, 
        ext_code,
        -- label_value, 
        sku, 
        timestamp,
        -- brand_id, 
        category_id,
        -- currency_id,
        -- label_uom_id, 
        -- manufacturer_id, 
        -- sales_uom_id, 
        status_id
        -- image 
        )
        VALUES (%s, %s, %s, %s, %s, %s, %s, %s)



